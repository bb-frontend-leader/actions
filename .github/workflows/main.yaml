name: Test

on: [push]

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.7
    
      - name: 'run-script 🔥'
        run: |
            chmod +x script.sh
            ./script.sh